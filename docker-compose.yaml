services:
  cajunlyrics-backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: cajunlyrics-api:latest
    container_name: api
    ports:
      - "3000:3000"
    networks:
      - cajunlyrics-network

  # Placeholder for the frontend service
  # cajunlyrics-frontend:
  #   build:
  #     context: ../cajunlyrics-frontend
  #     dockerfile: Dockerfile
  #   container_name: web
  #   ports:
  #     - "8080:80"
  #   networks:
  #     - cajunlyrics-network
  #   # Uncomment the following lines when the frontend is ready
  #   depends_on:
  #     - cajunlyrics-backend

networks:
  cajunlyrics-network:
    driver: bridge